RewriteEngine On

# Если файл или папка существует — отдать напрямую
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Всё остальное идёт на index.php
RewriteRule ^ index.php [QSA,L]